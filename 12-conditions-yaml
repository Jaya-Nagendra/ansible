- name: simple condition
  host: localhost
  vars:
    NAME: DevOps
  tasks:
  - name: run this if name is Devops
    ansible.builtin.debug:
      msg: "Hello {{NAME}}"  
    when: {{NAME}} == "DevOps"  